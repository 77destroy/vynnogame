# vynnogame

## Сайт-визитка «Линия»

Одностраничный статический сайт: `index.html`, `styles.css`, `main.js`, шрифты в `fonts/`.

Локальный просмотр из корня репозитория:

```bash
python3 -m http.server 8080
```

Откройте http://localhost:8080/
